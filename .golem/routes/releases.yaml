name: releases
path: /releases
model: "*Release"
rest: true
routes:
  - name: popular
    path: /popular/:interval
    method: GET
    result: "*PopularResponse"
    params:
      - name: interval
        type: string
